using UnityEngine;

public class SlotHitBox : MonoBehaviour
{
    public CassetteSlotHandler slotHandler;

    public CassetteSlotHandler GetSlotHandler()
    {
        return slotHandler;
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
